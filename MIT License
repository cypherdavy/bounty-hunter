
---

### **3. `main.py`**

The main entry point that will tie everything together:

```python
import argparse
from reconnaissance.recon import subdomain_enumeration
from scanners.zap_scan import zap_scan
from exploitation.exploit import exploit_xss
from reporting.report_generator import generate_report

def main(target):
    print(f"Starting reconnaissance for {target}")
    subdomains = subdomain_enumeration(target)
    scan_results = zap_scan(target)
    print(f"Scan complete for {target}. Found {len(scan_results)} issues.")
    
    # Exploit found vulnerabilities
    for result in scan_results:
        if result['type'] == 'XSS':
            exploit_xss(result['url'], result['payload'])

    # Generate report
    generate_report(scan_results)
    print("Scan and exploitation complete. Report generated.")

if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="Bounty Hunter - Automated Bug Hunting Tool")
    parser.add_argument("target", help="Target URL or domain")
    args = parser.parse_args()
    main(args.target)
